<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Xero Dashboard</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="app">
    <div class="topbar">
      <a href="/" class="brand">
        <div class="logo">📊</div>
        <span>Xero Dashboard</span>
      </a>
      <div class="topmeta">
        <% if (connected) { %>
          <span class="pill connected">● <%= tenantName %></span>
        <% } else { %>
          <span class="pill disconnected">○ Not Connected</span>
        <% } %>
      </div>
    </div>
    
    <div class="main">
      <div class="sidebar">
        <a href="/" class="navbtn <%= active === 'home' ? 'active' : '' %>">
          <span class="navicon">🏠</span>
          <span>Home</span>
        </a>
        
        <% if (connected) { %>
          <a href="/dashboard" class="navbtn <%= active === 'dashboard' ? 'active' : '' %>">
            <span class="navicon">📊</span>
            <span>Dashboard</span>
          </a>
          <a href="/invoices" class="navbtn <%= active === 'invoices' ? 'active' : '' %>">
            <span class="navicon">📄</span>
            <span>Invoices</span>
          </a>
          <a href="/contacts" class="navbtn <%= active === 'contacts' ? 'active' : '' %>">
            <span class="navicon">👥</span>
            <span>Contacts</span>
          </a>
          <a href="/accounts" class="navbtn <%= active === 'accounts' ? 'active' : '' %>">
            <span class="navicon">🏦</span>
            <span>Accounts</span>
          </a>
        <% } %>
        
        <a href="/tokens" class="navbtn <%= active === 'tokens' ? 'active' : '' %>">
          <span class="navicon">🔑</span>
          <span>Tokens</span>
        </a>
        <a href="/settings" class="navbtn <%= active === 'settings' ? 'active' : '' %>">
          <span class="navicon">⚙️</span>
          <span>Settings</span>
        </a>
        
        <div class="sidebar-footer">
          Callback:<br>
          <code><%= redirectUri %></code>
        </div>
      </div>
      
      <div class="content">
        <div class="container">
          <%- body %>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
